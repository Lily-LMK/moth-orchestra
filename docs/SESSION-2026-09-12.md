# Reference capture and first synchrony repairs — 12 September 2026

Scope: capture the current performance, write regression tests before repairs, and begin the agreed reliability work. All work is inside the Moth Orchestra project folder. Historical standalone versions and the new export were preserved. Maintained source is `repository/index.html`; branch is `repair/synchrony-reference`. Nothing was pushed or deployed.

## Reference

Baseline is original Git commit `b6663f11531e42593b149685d3c454975cd61a18`, copied to `../reference-2026-09-12/baseline.html` before app edits. Each mode has a stereo 44.1 kHz WAV, an event-score/settings JSON, and a still screenshot. Settings: embedded 48-observation sample on 2026-01-28, seed 1, Both, 19-second loop, D pentatonic, Class, mixed instruments, no ambience, master volume 1; Riff 19:00–20:40 Brisbane. WAV length is 21.5 seconds including tail. Still screenshots do not establish animation timing. Noise and particles are not entirely seeded.

The reference-player copy includes test-only setup/capture controls. The separate baseline.html is unchanged. The repaired capture player has the same test setup. Controls in a capture copy can be altered interactively, so consult its saved JSON when comparing.

## Deliberate changes

| Sample, seed 1 | Before | After |
|---|---:|---:|
| Timeline/Riff observation notes | 48 | 48 |
| Timeline/Riff shared-minute bridges | 8 | 8 |
| Timeline/Riff near-simultaneous pulses | 19 | 8 |
| Song observation notes | 74 | 74 |
| Song events labelled synchrony pulses | 40 | 0 |
| Song genuine shared-minute bridges | 0 | 8 |
| Song separately identified accompaniment | 0 | 32 |

Song keeps its complete scheduled score, including accents and accompaniment loudness; eight existing matching accents now trigger genuine shared-minute bridges, and 32 accompaniment events draw neutral dots instead of synchrony bursts. Timeline/Riff lose unsupported extra pulses, an intentional audible change. Their ordinary observation notes are unchanged. Evidence survives on matching events and in an expandable shared-minute list. Riff and focus use Brisbane civil time. Third observers cannot impersonate A. Solo player changes now rebuild every mode so returning to Both restores B and evidence.

## Tests and browser checks

- Initial synchrony red phase: 35 failing tests, eight future TODOs, saved alongside the baseline.
- Final suite: **53 passing, zero failing, eight TODOs**. Run `node --test tests/*.test.cjs`.
- Three additional actual control-handler regressions cover Both → solo → rebuild → Both. Two failed before the final control repair; all pass after it.
- Covers matching/nonmatching times, offsets, minute boundaries, duplicates, missing/invalid times, additional observers, solo, empty data, Riff boundaries and midnight windows, original-time near pulses, loop invariance, source immutability, and original musical-score equality. Riff checks run under UTC, Brisbane and New York host timezones.
- Browser: all modes loaded and offline WAVs rendered; shared-minute evidence expanded; Song playback started; Timeline/Riff solo switching and restoration exercised; playback observed beyond a loop; no warning/error logs returned in final checks. Pausing on a mode switch is existing behavior, not sustained-playback acceptance.
- Not established: subjective musical acceptance, exact flourish/audio alignment, all control/scheduler combinations, actual iPhone behavior, or sustained festival reliability. The automated harness does not exercise Web Audio or canvas rendering; browser captures provide separate smoke checks.

## New export

`observations-780797.csv`: 6,760 rows, no duplicate IDs, Chris Burwell 3,553 / Lily Kumpe 3,207; all timezone labels Brisbane. Dates 2020-04-05 through 2026-09-11. Four rows lack timestamps; 6,756 rows parse across 427 dates. The original file is untouched.

All parsed dates built in Timeline, Riff and Song with no non-finite event times or frequencies. Genuine shared-minute totals: Timeline 312; default Riff 274; Song 312. This validates parsing/sequencing, not full interactive CSV import or taxonomy enrichment. Raw file profile and complete build totals are saved outside the checkout in the reference/comparison folders. The app still needs a visible omitted-row summary; the current importer silently omits missing timestamps.

Both original and repaired Song WAVs reach the 16-bit PCM limit at master volume 1. This indicates a pre-existing export-headroom issue; it was preserved, not repaired, in this first synchrony pass. Riff/Timeline captures peak below that limit.

## Next steps

1. Listen to the saved before/after WAVs, especially reduced Timeline/Riff pulses, and inspect the revised Song flourish.
2. Implement the optional long-gap control separately: eight TODO descriptions are placeholders, not implemented test coverage. Write executable failing remapping tests first.
3. Broaden browser verification for loop boundaries, controls and scheduling; test on a real iPhone.
4. Address import omission feedback, explicit observer/session choices if needed, and licence/attribution before release.
5. Review the feature branch, then decide whether to push and open a PR. Merging main would deploy.

The existing `docs/PLAN.md` was already present and untracked when this session began; its broader first-release scope remains future work. V2 layout differences remain unreviewed.
